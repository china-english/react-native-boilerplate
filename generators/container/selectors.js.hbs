/*
 *
 * {{ properCase name }} selectors
 * created by generator
 *
 * source => https://github.com/china-english/react-native-boilerplate
 * author => fei
 *
 */

import { createGetSelector } from 'reselect-immutable-helpers';
import Immutable from 'immutable';
// import { createSelector } from 'reselect';

/*
 * Direct selector to the {{ camelCase name }} reducer
 * 直接挑选 {{ camelCase name }}Reducer
 */
export const select{{ properCase name }}Reducer = (state) => state.get('{{ camelCase name }}', Immutable.Map());

/*
 * selector data from {{ camelCase name }}Reducer object
 * 选择其中的特定字段
 */
export const selectGenerateText = createGetSelector(
  select{{ properCase name }}Reducer, 'generateText'
);

/*
 * 使用 createSelector 对数据进行操作, 如下所示
 */
// export const selectFormatString = createSelector(
//   selectGenerateText,
//   (generateText) => generateText
// );
