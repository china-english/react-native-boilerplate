/*
 *
 * {{ properCase name }} selectors test file
 * created by generator
 *
 * source => https://github.com/china-english/react-native-boilerplate
 * author => fei
 *
 */

import { fromJS } from 'immutable';
import { select{{ properCase name }}Reducer } from '../selectors';

describe('select{{ properCase name }}Reducer', () => {
  it('Tests specified unit', () => {
    const initialState = fromJS({});
    const expected = fromJS({});
    expect(select{{ properCase name }}Reducer(initialState)).toEqual(expected);
  });
});
