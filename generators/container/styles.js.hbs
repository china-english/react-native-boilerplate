import { Dimensions } from 'react-native';
import { grey200 } from '../../theme/variables/commonColor';

const { height } = Dimensions.get('window');

export default {
  contentContainer: {
    justifyContent: 'center',
  },
  content: {
    backgroundColor: grey200,
  },
  contentView: {
    backgroundColor: white,
    height: height{{#if wantHeader}} - 64{{/if}}{{#if wantFooter}} - 55{{/if}},
    justifyContent: 'center',
  },
  button: {
    alignSelf: 'center',
    marginBottom: 10,
  },
  footerTble: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  generateText: {
    textAlign: 'center',
    marginVertical: 10,
  },
};
