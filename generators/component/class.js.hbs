/**
 *
 * {{ properCase name }} Component
 * created by generator
 *
 * source => https://github.com/china-english/react-native-boilerplate
 * author => fei
 *
 */
{{#if wantTransLate}}

/* global translate */
{{/if}}

import React from 'react';
import PropTypes from 'prop-types';
import {
  View,
  Icon,
  Text,
} from 'native-base';

import styles from './styles';

class {{ properCase name }} extends {{{ type }}} { // eslint-disable-line
  // constructor(props) {
  //   super(props);
  //   this.state = {};
  // }

  render() {
    const { generateText } = this.props;
    return (
      <View style={styles.content}>
        <Icon type="FontAwesome" name="smile-o" style={styles.icon} />
        {{#if wantTransLate}}
        <Text style={styles.text}>{translate(generateText)}</Text>
        <Text style={styles.text}>{translate('componentMessage')}</Text>
        {{else}}
        <Text style={styles.text}>{generateText}</Text>
        <Text style={styles.text}>This component is generated by the generator</Text>
        <Text>此组件由生成器生成</Text>
        {{/if}}
      </View>
    );
  }
}

{{ properCase name }}.defaultProps = {
  generateText: 'welcome',
};

{{ properCase name }}.propTypes = {
  generateText: PropTypes.string,
};

export default {{ properCase name }};
